<%- include('../layout', { body: `
<div class="max-w-2xl mx-auto">
  <div class="bg-white rounded-xl shadow-lg p-6 mb-6">
    <div class="mb-6">
      <h2 class="text-2xl font-bold text-gray-800 flex items-center gap-3">
        <i class="fas fa-money-bill-wave text-primary-500"></i>
        Configuración de Retiros
      </h2>
      <p class="text-gray-600 mt-2">Establece el monto máximo de efectivo permitido en caja. Cuando se alcance este límite, se solicitará realizar un retiro antes de procesar cobros en efectivo.</p>
    </div>

    ` + (success ? `
    <div class="mb-4 bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded">
      ` + success + `
    </div>
    ` : '') + `

    ` + (error ? `
    <div class="mb-4 bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded">
      ` + error + `
    </div>
    ` : '') + `

    <form method="POST" action="/configuracion/retiros" class="space-y-6">
      <div>
        <label for="montoMaximoEfectivo" class="block text-sm font-medium text-gray-700 mb-2">
          Monto Máximo de Efectivo en Caja ($)
        </label>
        <div class="relative">
          <span class="absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-500 text-lg">$</span>
          <input 
            type="number" 
            id="montoMaximoEfectivo" 
            name="montoMaximoEfectivo" 
            step="0.01" 
            min="0" 
            value="` + (configRetiros ? parseFloat(configRetiros.montoMaximoEfectivo).toFixed(2) : '0.00') + `"
            required
            class="w-full pl-8 pr-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent text-lg"
            placeholder="0.00"
          />
        </div>
        <p class="mt-2 text-sm text-gray-500">
          <i class="fas fa-info-circle mr-1"></i>
          Establece el límite máximo de efectivo permitido en caja. Cuando el saldo de efectivo alcance o supere este monto, se solicitará realizar un retiro antes de procesar nuevos cobros en efectivo.
          <br><br>
          <strong>Nota:</strong> Si estableces $0.00, no habrá límite de efectivo.
        </p>
      </div>

      <div class="flex gap-4">
        <button 
          type="submit" 
          class="flex-1 bg-primary-600 hover:bg-primary-700 text-white py-3 px-6 rounded-lg transition font-medium shadow-md hover:shadow-lg"
        >
          <i class="fas fa-save mr-2"></i>
          Guardar Configuración
        </button>
        <a 
          href="/configuracion" 
          class="flex-1 bg-gray-200 hover:bg-gray-300 text-gray-700 py-3 px-6 rounded-lg transition font-medium text-center"
        >
          <i class="fas fa-arrow-left mr-2"></i>
          Volver
        </a>
      </div>
    </form>
  </div>
</div>
` }) %>


